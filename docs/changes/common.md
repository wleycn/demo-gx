# common changes (`src/demo_gx/common/`)

> Format and discipline: see `docs/rules/DEVELOP-FLOW.md` section 4. Append-only; never rewrite past entries.

(no entries yet: this directory records changes from its establishment onward; for prior history see `git log`)

## 20260914 · pii-masking-at-ingestion — Pointer

- New `mask.py` in this directory is recorded in full in `docs/changes/ingestion.md`, together with the boundary wiring and the contract updates it required.
