# engineering changes (build / deps / config / docs / tooling / gate)

> Format and discipline: see `docs/rules/DEVELOP-FLOW.md` section 4. Append-only; never rewrite past entries.

## 20260914 · extract-error-envelope — Pipeline entry point change

- **Motivation**: part of the `#cli-holds-transform` migration. Full entry lives in `docs/changes/validation.md`.
- **Scope**: `pipeline/cli.py` only — the inline error-envelope construction was replaced by a call to `validation.error_envelope.write_error_envelope`, plus one import. `cli.py` is the pipeline entry point and has no module directory of its own, so the pointer is recorded here.
- **Behaviour and contract changes**: none.
- **Verification**: see the full entry in `docs/changes/validation.md`.
- **Rollback**: revert the commit.
- **Related**: `docs/changes/validation.md` (same date) / `KNOWN-ISSUE.md#cli-holds-transform`.

## 20260914 · inject-run-timestamp — Inject the run timestamp at the entry boundary

- **Motivation**: close the registered deviation "partition parameter injection" (`AGENTS.md` section 10). Four places stamped data from the system clock, so re-running the same batch was not reproducible and the validator's future-timestamp rule depended on when it ran. Two of those four were outside the originally reported site; the whole class was fixed rather than only the reported pair.
- **Scope**: `pipeline/cli.py` (new `--run-timestamp` flag, the run instant resolved once at the top of `main()` and threaded down); `pipeline/ingestion/reader.py` (`write_bronze(raw_df, bronze_base, run_ts)`); `pipeline/validation/schema_validator.py` (`SchemaValidator(schema_config, run_ts)`); `tests/test_validation.py` (fixed `RUN_TS` constant, `_ts(days_ago)` fixture helper, 16 constructor call sites, 8 timestamp literals); `docs/business/INTERFACE-DESIGN.md` section 1; `docs/business/MODULE-DESIGN.md` sections 1, 2.1, 2.2 and 2.9; `docs/business/KNOWN-ISSUE.md`; `docs/business/PROJECT.md`; `AGENTS.md` sections 1 and 10; `docs/changes/ingestion.md` and `docs/changes/validation.md` (pointer entries).
- **Behaviour and contract changes**: two module signatures changed and are now required parameters. `write_bronze` takes `run_ts`; `SchemaValidator.__init__` takes `run_ts`. The CLI gains an optional `--run-timestamp`. Omitting the flag preserves the previous behaviour, apart from the read happening once at the boundary instead of at each stamp site. No output column, partition scheme, or file naming changed.
- **Verification**: `.venv/bin/python -m pytest tests/ -q` → 27 passed. Behaviour equivalence against the previous revision: 100 artefact fingerprints identical after dropping run-timestamp columns. Reproducibility proved: two runs with `--run-timestamp 2026-09-14T00:00:00Z` produced byte-identical data artefacts (single aggregate hash `13d48407e55bdb13`); two runs without the flag differed, as expected. Injected value confirmed present in Silver `_processed_timestamp`, envelope `ingestion_timestamp`, and the envelope filename `20260914T000000000000Z_errors.json`.
- **Rollback**: revert the commit. The `--run-timestamp` flag disappears with it and callers go back to the wall clock. No data repair needed.
- **Related**: `CHANGELOG.md` entry for this date / `KNOWN-ISSUE.md#now-timestamp` / commit hash in `git log`.

## 20260914 · src-layout-package — Move to an installable src/demo_gx package

- **Motivation**: close the registered deviation "source directory layout" (`AGENTS.md` section 10). `pipeline/` used flat namespace packages with no `__init__.py`, so the code could not be installed and imports depended on the working directory being the project root. The same pass recorded the remaining deltas between this project and the data-processing structural skeleton.
- **Scope**: `pipeline/` to `src/demo_gx/` via `git mv` (history preserved) plus `__init__.py` in the package and each subpackage; 17 intra-package imports rewritten to `demo_gx.*`; `common/config.py` root anchor `parents[2]` to `parents[3]`; `common/logger.py` logger name `pipeline` to `demo_gx` behind a `LOGGER_NAME` constant; new `pyproject.toml`; removed `requirements.txt` and `pytest.ini`, both consolidated into `pyproject.toml`; `Makefile`; `.gitlab-ci.yml`; `.gitignore`; `README.md`; `AGENTS.md` sections 1, 6, 9.1 and 10; `docs/business/PROJECT.md`, `MODULE-DESIGN.md`, `INTERFACE-DESIGN.md`, `DOMAIN-LANGUAGE.md`, `KNOWN-ISSUE.md`; the six `docs/changes/*.md` file headers.
- **Behaviour and contract changes**: the entry point is now `python -m demo_gx.cli` instead of `python pipeline/cli.py`. Dependencies moved from `requirements.txt` to `pyproject.toml`; the install step is `pip install -e ".[dev]"`. Log lines carry `"logger": "demo_gx"` instead of `"pipeline"`. No data output, partition scheme, or CLI flag changed.
- **Verification**: `.venv/bin/python -m pytest tests/ -q` → 27 passed, run both from the project root and from `/tmp`. `python -m demo_gx.cli --input data/sample_data.json --env test` → exit 0. Import from `/tmp` resolves the package and yields `PROJECT_ROOT = /home/hermes/workspace/demo-gx`. Behaviour equivalence against the previous revision: 100 artefact fingerprints identical after dropping run-timestamp columns. Reproducibility unchanged: two injected-timestamp runs share the aggregate hash `13d48407e55bdb13`.
- **Rollback**: revert the commit. `git mv` reverses cleanly and `requirements.txt` / `pytest.ini` return. No data repair needed.
- **Related**: `CHANGELOG.md` entry for this date / `KNOWN-ISSUE.md#layout-flat-pipeline` / commit hash in `git log`.

> Historical entries in this directory and in `validation.md` / `ingestion.md` name files under `pipeline/`. That was this package's path before 20260914. Entry bodies are append-only and are not rewritten.

## 20260914 · verification-correction — Correct the verification claims in the three entries above

- **Motivation**: an independent read-only audit of the three entries above found verification claims a reader cannot reproduce as written. Entry bodies stay untouched because this directory is append-only; the corrections are recorded here instead.
- **Scope**: documentation only, plus one comment in `scripts/generate_sample_data.py`. No pipeline code, config, or output shape changed.
- **Behaviour and contract changes**: none. The audited re-runs confirmed every underlying behaviour; what was wrong was the wording of the evidence.
- **Verification**: four corrections. Each was re-run before being written here.
  1. **The aggregate hash `13d48407e55bdb13` is not reproducible and is withdrawn.** It was measured against a `data/sample_data.json` that no longer exists: `make run` depends on the `data` target, which regenerates that file, and `scripts/generate_sample_data.py` varies `event_id` (uuid4) and every timestamp per run. The reproducible statement is the invariant, with the fixture held fixed: two runs of `rm -rf test/data && .venv/bin/python -m demo_gx.cli --input data/sample_data.json --env test --run-timestamp 2026-09-14T00:00:00Z` give identical `sha256sum` manifests over `find test/data -type f ! -path "*/logs/*" ! -name metrics.json` — 101 files, empty `diff`.
  2. **The artefact count in the equivalence check is fixture-dependent, not a fixed 100.** The count in the first entry was measured on the fixture present then. The method is the reproducible part: `git stash` the change, run both revisions against the same fixed fixture, compare fingerprints after dropping run-timestamp columns. Every fingerprint matched.
  3. **"27 passed from `/tmp`" needs the absolute path**: `cd /tmp && /home/hermes/workspace/demo-gx/.venv/bin/python -m pytest /home/hermes/workspace/demo-gx/tests -q` gives 27 passed. A bare `pytest tests/` from `/tmp` collects nothing.
  4. **The end-to-end command is `python -m demo_gx.cli`** after the package move. `pipeline/cli.py` no longer exists, so the command quoted in the first two entries no longer runs as written.
- **Rollback**: not applicable; documentation only.
- **Related**: `docs/changes/validation.md` (same date, correction entry) / commit hash in `git log`.

## 20260914 · english-doc-assets — Move the whole repository to English

- **Motivation**: the repository is delivered to English-speaking readers, but eight tracked files still carried Chinese. Four of them (`AGENTS.md` and the `docs/rules/` set) are assembled verbatim from the upstream material library, so editing them in place would have forked the single source and broken the assembly-consistency check. The upstream library therefore gained a language layer, and this repository was re-assembled from it.
- **Scope**: upstream material library — new `references/rules/en/` and `references/AGENTS/en/` mirror layers (11 files); `rules_assembly.py` (`--lang`, sparse-mirror resolution, fail-closed preflight, cross-language skeleton parity); `agents_assembly.py` (`--lang`, per-language section list, cross-language section-number parity); `verify_rules_layering.py` (`--lang`, per-language type keywords and skip markers); `pre_commit_gate.py` (the `AGENTS.md` section check is now by section number and therefore language-neutral). This repository — `AGENTS.md` regenerated, the four `docs/rules/` files re-assembled, the change-entry field labels in `docs/changes/` translated, `README.md` gate description corrected, `docs/business/INTERFACE-DESIGN.md`, `docs/business/PROJECT.md`, `docs/business/KNOWN-ISSUE.md`, `docs/changes/engineering.md`.
- **Behaviour and contract changes**: none in the pipeline; documentation and tooling only. Two contract-level points. First, the change-entry field labels are now English: Motivation / Scope / Behaviour and contract changes / Verification / Rollback / Related. Second, `AGENTS.md` section 10 gained a deviation row for the remaining telemetry clock reads, plus cost-and-rollback notes for every row.
- **Verification**: `.venv/bin/python -m pytest tests/ -q` to 27 passed. `pre_commit_gate.py --repo . --agents-strict` to exit 0 with zero warnings. `rules_assembly.py --stack python --type data-processing --lang en --check .` to all four files identical to the upstream assembly. `verify_rules_layering.py --lang en --project . --stack python --type data-processing` to B1/B2/C1/D1/E1 all zero. `agents_assembly.py --type data-processing --lang en --check` to pass, including the cross-language section-number parity check. Chinese-character scan over all 63 tracked files to zero. Line endings: every `.md` CRLF, every code and config file LF.
- **Rollback**: revert the commit. The pipeline code is untouched, so no data repair is needed. Reverting restores the Chinese assets as well, because the English layer lives in the upstream material library rather than in this repository.
- **Related**: `CHANGELOG.md` entry for this date / `docs/changes/validation.md` and `docs/changes/ingestion.md` (pointer entries, same date) / commit hash in `git log`.

## 20260914 · layer-label-drift — Fix the layer label drift in the assembly tool

- **Motivation**: `rules_assembly.py` emitted enriched layer labels such as `python 栈` and `项目类型：website`, while every assembled file in both live projects uses the bare layer directory name and the tool's own docstring documents the bare name. The `--check` mode therefore failed on both projects before this change. The projects, the docstring and the full assembly history agree with each other, so the tool was the outlier and was corrected.
- **Scope**: `ng/tools/rules_assembly.py` (`layer_label` and its call site, plus the docstring). No file in this repository changed for this fix.
- **Behaviour and contract changes**: none. Assembled layer headings read `【层：python】` and `【层：data-processing】` again, matching every artefact ever produced.
- **Verification**: `rules_assembly.py --stack python --type website --check <memo-ledger>` to PASS and `--stack python --type data-processing --check .` to PASS. Both reported four mismatched files before the fix.
- **Rollback**: revert. Recorded here because this tool produces this repository's `docs/rules/` set, even though it lives outside the repository.
- **Related**: commit hash in `git log`.

## 20260914 · gate-type-marker-languages — Teach the type-marker check the English markers

- **Motivation**: the language migration produced English `docs/rules/` materials in which the abolished type marker is spelled `[data-type]` rather than `[数据类]`. The gate's type-marker residue check only knew the three Chinese spellings, so a residue in an English project would have gone unnoticed. The check now knows both language layers.
- **Scope**: `ng/tools/pre_commit_gate.py` (`TYPE_MARKS`). No file in this repository changed for this fix.
- **Behaviour and contract changes**: none for either live project. A mention of the marker inside a blockquote stays exempt, so the upstream materials that describe the marker do not trip the check.
- **Verification**: `pre_commit_gate.py --repo . --agents-strict` to exit 0 and `--repo <memo-ledger>` to exit 0, so no false positive was introduced. A negative fixture with `[data-type]` in prose now reports "残留类型标记 ['[data-type]']", which it did not before.
- **Rollback**: revert. Recorded here because this gate decides whether this repository's commits are blocked.
- **Related**: `docs/changes/engineering.md` (same date, `english-doc-assets`) / commit hash in `git log`.

## 20260914 · line-ending-hygiene — Fix the double-CR line endings and add the byte-level check

- **Motivation**: `AGENTS.md` carried `\r\r\n` (a double carriage return) at the end of every line in the working tree, so most renderers displayed a blank line after each line. `git status` and `git diff` both reported the file as clean, because the `eol=crlf` attribute in `.gitattributes` normalises line endings before comparing. A fresh clone reported the file as modified instead, because the index held an unnormalised blob. "Extra blank lines" had been reported before on other documents, and this case explains why they survive review.
- **Scope**: this repository — `AGENTS.md` working tree rewritten to CRLF and re-added, the `docs/rules/` set re-assembled. Upstream material library — `pre_commit_gate.py` gained check 10 (line-ending hygiene at the byte level, whole repository); `_base/pre-commit-gate/README.md` gained the two missing rows for checks 9 and 10; `_base/ACCEPTANCE-CHECKLIST.md` and its `en/` mirror gained one checkpoint; skill `project-doc-system` and skill `docs-writing-discipline` document the new check. The word-list rollout never reached the rules layer for check 9, which is why that row was missing.
- **Behaviour and contract changes**: none in the pipeline; documentation and tooling only. One acceptance criterion is added. A merge request now checks line endings and blank lines as well.
- **Verification**: `git clone` of the repository into a temporary directory reported an empty `git status`, and `AGENTS.md` arrived as pure CRLF; before the fix the same clone reported `M AGENTS.md`. `pre_commit_gate.py --repo . --agents-strict` to exit 0 with zero warnings, and the same result for the other live project. `pytest tests/ -q` to 27 passed. The gate counterexample probe reported all eight expected hits and all five expected non-hits. A repository-wide scan found no other file with a double CR.
- **Rollback**: revert the commits. No data repair is needed. The new gate check is a warning, except that `--eol-strict` promotes the double-CR finding to a blocker.
- **Related**: `CHANGELOG.md` entry for this date / commit hashes in `git log`.

## 20260914 · eol-check-blocking — Make the double-CR finding a blocker, and keep the archive out of scope

- **Motivation**: the line-ending check added earlier the same day reported a double CR as a warning. That defect is invisible to git, so this check is the only thing that can catch it, and a warning is easy to scroll past. The byte pattern has no legitimate use in a text file, and the check produced zero false positives across both live projects and the whole machine.
- **Scope**: `ng/tools/pre_commit_gate.py` — the double-CR finding blocks by default, `--eol-strict` is replaced by `--eol-warn` as the downgrade escape, and the scan now skips the archive area through the same `ARCHIVE_SKIP_RX` constant that the writing check uses. Upstream material library — the gate README row and the two skills that describe the check. No file in this repository changed for this fix.
- **Behaviour and contract changes**: a commit that introduces a double CR outside the archive area is now rejected. The remaining line-ending findings stay at warning level.
- **Verification**: both live projects to exit 0 with blocking enabled. A machine-wide scan of 18417 text files found no double CR outside the archive area, so the new blocker cannot fire on existing content. The counterexample probe now asserts the exit code as well as the message; it reported eight expected hits, six expected non-hits, and a non-zero exit against a repository that contains a double-CR sample.
- **Rollback**: revert the commit. `--eol-warn` downgrades the check in place, and `--no-eol` skips it entirely.
- **Related**: `docs/changes/engineering.md` (same date, `line-ending-hygiene`) / `CHANGELOG.md` / commit hash in `git log`.

## 20260914 · env-storage-layout — One root per environment, split by direction

- **Motivation**: the three storage roots sat side by side as `data/`, `test/data/` and `data_prod/`. Three problems came with that shape. Each environment became a separate top-level concept. The test root sat next to `tests/`, differing by one letter. The input file had no defined home, so the sample lived inside the dev artefact root while the CI smoke run read it against the test environment.
- **Scope**: `config/{dev,test,prod}.yaml` (root plus `input_subpath` / `input_file` / `output_subpath`); `common/config.py` (`input_root` and `output_root` derived once, `_anchor_to_base` renamed to `_anchor_to_output`); `cli.py` (artefact paths join `output_root`, `--input` becomes optional); `scripts/generate_sample_data.py` (`--env` / `--output` / `--records` added, every draw made deterministic); `Makefile`; `.gitlab-ci.yml`; `.gitignore`; `.gitattributes`; the committed layer skeleton under `data/`; `AGENTS.md`, `README.md` and four `docs/business/` documents.
- **Behaviour and contract changes**: layout only, plus one contract addition. `--input` changes from required to optional and defaults to `<env root>/input/sample_data.json`; passing it explicitly behaves exactly as before, so the addition is backward compatible. No output column, partition scheme or file name changed. `make clean` now preserves the committed skeleton and the sample inputs, where it previously deleted the whole storage root.
- **Verification**: all three environments run to exit 0. `pytest` reports 27 passed. Equivalence was proved by an A/B run. One fixed input and one pinned `--run-timestamp` produced 102 byte-identical artefacts under both layouts, with matching counters: input 107, valid 102, invalid 5, silver 101, gold 100, bronze 107, duplicates 1. The sample generator is byte-stable, so three consecutive runs give one sha256. A full pipeline run over all three environments leaves `git status` clean, and `make clean` restores it.
- **Incidental finding**: the validator rejects any record whose `ingestion_timestamp` is later than the run instant. Because the generator used to stamp records with the wall clock, every documented replay command that pins `--run-timestamp` stopped reproducing once the clock moved past it. Fixing the base instant removed that decay; the rule itself is unchanged and is now noted in the generator docstring.
- **Rollback**: revert the commits. The layout moves back and `--input` returns to required. Run artefacts are rebuildable, so no data repair is needed and only the committed sample would need regeneration.
- **Related**: `CHANGELOG.md` entries for this date / `docs/business/INTERFACE-DESIGN.md` section 3 / commits `13a6654`, `f28f68e`, `3b7711f`, `1f91b73`.

## 20260914 · table-contract-completion — Complete the table contracts and pull table-level detail back into them

- **Motivation**: a review found that `docs/tables/` and `docs/business/DATA-DESIGN.md` both carried table-level detail, so the same facts appeared in two places. The contracts also fell short of their own rule. The data-processing structural rule requires twelve items per contract, and the five contracts carried five of them. Missing were subject, partition rationale and volume, the monetary convention, a PII section, freshness SLA and owner, dependencies, and a quality-rule list. Without those items a contract read like a thin shadow of DATA-DESIGN.md, which is what made the overlap look total.
- **Scope**: `docs/business/DATA-DESIGN.md` sections 2.3, 2.4 and 2.7; all five files under `docs/tables/`. Documentation only.
- **Behaviour and contract changes**: none to code, config or output. The documentation boundary is now explicit. A layer that has a contract file keeps only the global view in DATA-DESIGN.md and points to the contract for table-level detail. Bronze has no contract file, so DATA-DESIGN.md stays its single definition. Cross-table semantics such as reprocessing and schema evolution also stay there.
- **Verification**: each of the five contracts now carries all twelve required items, 5 of 5. The six changed files hold CRLF with no bare LF and no double CR, and carry no CJK. `pytest` reports 27 passed. The pre-commit gate reports exit 0 over the six staged files. Table-level duplication dropped: the Silver write mode and deduplication text left DATA-DESIGN.md section 2.3, and the grain and partition columns left the section 2.4 summary table. The remaining shared wording is the global view, which is deliberate.
- **Incidental finding**: DATA-DESIGN.md section 2.7 still named the artefact paths that the environment-storage-layout change had replaced, so one document carried two shapes at once. The table now states its base path once and lists the three rows relative to it.
- **Rollback**: revert the commit. No code or data depends on this documentation.
- **Related**: `CHANGELOG.md` entry for this date / `docs/rules/PROJECT-STRUCTURE.md` section 3 / `docs/business/DATA-DESIGN.md` sections 2.3, 2.4 and 2.7.

## 20260914 · retract-phantom-fields — Align the contracts with the artefacts and retire two fields that were never built

- **Motivation**: completing the table contracts exposed a drift. Two fields described in the documentation do not exist in the written artefacts, and two further surfaces repeated the same claim. The names came from the archived pre-migration design, which reserved them for a path the project later rejected. `_validation_status` belonged to the "yellow path" recorded under "Decision: Fail-safe isolation over flag-and-pass", and `category` was a placeholder that `curation/builder.py` mentions only in a comment.
- **Scope**: `docs/tables/silver_events.md` (one Field List row removed, and the list reordered to the artefact column order); `docs/tables/dim_event_type.md` and `docs/tables/wide_daily_user_events.md` (Field List, `category` removed); `docs/tables/dim_customer.md` (a Future-extension line named a column this table never had); `docs/business/KNOWN-ISSUE.md` (the reason text no longer claims the column exists); `docs/business/INTERFACE-DESIGN.md` (the type-change sentence no longer judges a row by that column); `src/demo_gx/transformation/cleaner.py` (docstring only, no logic touched).
- **Behaviour and contract changes**: none. No artefact gained or lost a column; only the descriptions moved.
- **Verification**: each contract's Field List now matches its artefact column by column and in order, 5 of 5. `pytest` reports 27 passed. The changed Markdown keeps CRLF with no bare LF and no double CR, the changed module keeps LF, and nothing carries CJK.
- **Decision**: the documentation follows the implementation. Adding the columns to satisfy the text was rejected: a column that always holds `"passed"` carries no information, and the change would invalidate the byte-identical artefact evidence from the storage-layout change.
- **Rollback**: revert the commit.
- **Related**: `CHANGELOG.md` entry for this date / `KNOWN-ISSUE.md` (Decision: Fail-safe isolation over flag-and-pass) / `docs/tables/`.

## 20260914 · dedup-rule-single-source — Make the table contract the single source for the deduplication rule

- **Motivation**: DATA-DESIGN.md section 1 carried the full Silver deduplication rule under "Key Flow Branches", and the Silver contract carried it a second time while pointing back at section 1. Two write surfaces meant no single source: a reader could not tell which copy to trust, and a change to one could leave the other silently stale. The other three branches in that section describe cross-table behaviour, so only this one belonged to a single table.
- **Scope**: `docs/business/DATA-DESIGN.md` section 1, where the Deduplication path entry collapsed to an overview plus a pointer; `docs/tables/silver_events.md`, where the Deduplication section now states the tie-break rule in full and no longer points back. Documentation only.
- **Behaviour and contract changes**: none. Both copies stated the same rule, and that rule matches `transformation/deduplicator.py`.
- **Verification**: the rule and its tie-break now appear in exactly one place. `grep` finds no `keep-last`, `stable sort`, or `last-occurring` wording left in DATA-DESIGN.md, and the only remaining mention of the tie-break there is the pointer itself. All ten cross-references from the contracts into DATA-DESIGN.md still resolve to a heading that exists. `pytest` reports 27 passed. The gate reports exit 0. Both changed files keep CRLF with no bare LF and no double CR, and carry no CJK.
- **Rollback**: revert the commit.
- **Related**: `CHANGELOG.md` entry for this date / `docs/tables/silver_events.md` / `docs/business/DATA-DESIGN.md` sections 1 and 2.3.

## 20260914 · agents-deviation-record-move — Move the deviation record out of AGENTS.md and restore what the move dropped

- **Motivation**: `AGENTS.md` §10 carried the deviation record itself: the deviation table, the skeleton and stage-model applicability boundaries, the uncovered-module note and the cost-and-rollback notes. The project rule is that `AGENTS.md` is rules, not a log, so the record now lives in `docs/business/KNOWN-ISSUE.md` under "Skeleton Deviations" and §10 keeps the discipline plus a pointer. The move dropped content on both sides, which this entry closes.
- **Scope**: `AGENTS.md` §1, §9.1 and §10; `docs/business/KNOWN-ISSUE.md` (the "Skeleton Deviations" section: discipline list, stage-model boundary, cost-and-rollback summary); `docs/business/DOMAIN-LANGUAGE.md` (the `data layer mapping` row). Documentation only.
- **Behaviour and contract changes**: none to code, config or output. Three rule-level changes. First, the deviation discipline now states four conditions and is written in `KNOWN-ISSUE.md`, with §10 stating them in one line plus the pointer. Second, §10 states the structure explicitly as eleven headings, the ten core sections §0–§9 plus the pointer section. Third, §9.1 no longer claims the machine gate checks every path in the project map. The gate checks referenced files under `docs/`, `src/`, `scripts/`, `tests/` and `data/` plus the root files it names. A directory path or a `config/*.yaml` entry is therefore the reviewer's to verify.
- **Verification**: the deviation table carries 14 of 14 rows, verified row by row against the previous §10. Content dropped by the move is restored and present: the stage-model applicability boundaries, the eight per-row cost statements, the prohibition on a second anti-pattern table duplicating §3, and the eleven-heading structure. The `data layer mapping` row no longer points at §10, and no stale `AGENTS.md section 10` pointer remains outside the deliberate bridge sentence at `KNOWN-ISSUE.md` line 116. `.venv/bin/python -m pytest -q` reports 27 passed. The AGENTS check called directly reports no blockers and no warnings, and `python3 ng/tools/pre_commit_gate.py --repo . --tests-cmd ".venv/bin/python -m pytest -q" --agents-strict` reports exit 0 with no files staged and no warnings (its console output is Chinese). All three changed Markdown files hold CRLF with no bare LF and no double CR, carry no CJK (this change log keeps the Chinese tool output quoted by earlier entries), and their blank-line rate is 25%, 27% and 16%. The findings come from an independent three-lens audit run as separate read-only agents; their reports live outside the repository.
- **Open items (no action taken)**: the same audit reported three findings against red lines §3.1, §3.2 and §3.4 that this documentation change does not touch. `src/demo_gx/cli.py` builds Gold from every Silver partition on disk; `duplicates.log` is appended to rather than overwritten; and no masking exists for the sensitive fields that `docs/business/PROJECT.md` lines 77-78 describe, while `_raw_json` carries the raw record into Silver. Their disposition is pending a decision: register each as a deviation, or change the code.
- **Rollback**: revert the commit. No code or data depends on this documentation.
- **Related**: `CHANGELOG.md` entry for this date / `docs/business/KNOWN-ISSUE.md` section "Skeleton Deviations" / `docs/rules/DEVELOP-FLOW.md` section 4.
## 20260914 · one-statement-per-sentence — Split clause-chained sentences into one statement each

- **Motivation**: three business documents carried sentences that stated two or three things at once, joined by a semicolon, a colon or a relative clause. A reader met one sentence and had to work out which clause carried the rule. The edit arrived as an uncommitted working-tree change with no author record in git, so this entry gives it a home in the trail.
- **Scope**: `docs/business/DATA-DESIGN.md` (7 sentences), `docs/business/INTERFACE-DESIGN.md` (1) and `docs/business/MODULE-DESIGN.md` (6), the last of which includes one added sentence in the curation module description. Documentation only.
- **Behaviour and contract changes**: none. No rule, contract, config or code changed. The one added sentence states behaviour the code already has: `cli.py` builds Gold from every Silver partition on disk rather than from the in-memory batch, which `DATA-DESIGN.md` section 2.4 already explains.
- **Verification**: `git diff --stat` reports 3 files changed, 14 insertions and 14 deletions. `.venv/bin/python -m pytest -q` reports 27 passed, and the pre-commit gate reports exit 0. All three files keep CRLF with no bare LF and no double CR, and carry no CJK.
- **Open item (no action taken)**: that same Gold rebuild reads the full Silver snapshot, which red line section 3.2 forbids for queries. Recorded under the `agents-deviation-record-move` entry above; no disposition yet.
- **Rollback**: revert the commit.
- **Related**: `CHANGELOG.md` entry for this date / `docs/business/DATA-DESIGN.md` section 2.4.
## 20260914 · provenance-marker — Make the AI provenance marker a mechanism, not a habit

- **Motivation**: AGENTS.md section 5 required a `[AI-GENERATED]` comment on every changed file and an `[AI]` marker in the commit message. Neither ran anywhere: 0 of 22 code files carried the header, 0 of 43 commits carried the marker, and no check existed. A rule that rests on the executor remembering it fails silently, so the rule was rewritten and wired to exit codes.
- **Scope**: the upstream core template in both languages (`references/AGENTS/AGENTS.core.template.md` and its `en/` mirror, section 5 plus three sections aligned to the section-10 decision); `ng/tools/pre_commit_gate.py` (new `--commit-msg` mode, a code-header check, a missing-section-10 warning, and a deviation-anchor check); a new `ng/tools/test_provenance_gate.py`; this repository's `scripts/hooks/commit-msg`, its README section, and the AGENTS.md sections that the assembler now owns.
- **What the rule says now**: the header goes on changed **code** files only. An assembly product and a document are exempt, because their provenance is the commit marker plus the change trail. `reviewed_by=pending` marks a file no human has reviewed. An agent-session commit - detected by the `HERMES_AGENT` environment, not by asking - must carry `[AI]`; a human commit carries no marker.
- **Behaviour and contract changes**: an agent-session commit without `[AI]` is now blocked (exit 1). A changed code file without the header is reported as a warning, not blocked: 22 existing files carry no header, and a blocker must not fire on legacy content. The gate gained `--commit-msg`, so the pattern `git commit` path is unchanged for a human.
- **Verification**: `ng/tools/test_provenance_gate.py` runs 9 probes in both directions and all 9 pass. They cover: an agent message without `[AI]` blocks, with it passes; a human message is not asked; an empty message blocks; a code file without the header warns, with it clears; documents and assembly products are exempt; a missing section 10 warns; a deviation anchor that does not exist warns, while an identifier outside the deviation table does not. The probe found a real defect on its first run. The `--commit-msg` branch sat behind the `.git` guard, so it was skipped silently outside a repository, and the branch now runs first. `.venv/bin/python -m pytest -q` reports 27 passed. The three AGENTS.md regions this commit touches are byte-identical to the assembler output, asserted by the splice script rather than by eye.
- **Decision**: the header covers code only. A dated header in a rule document contradicts the rule that rules are not a log, and the gate already warns when `AGENTS.md` carries a date; an assembly product would also lose the header on the next reinstall. Documents keep their provenance in the commit marker and the change trail.
- **Open items (no action taken)**: 22 code files still carry no header, so the header check stays at warning level until they are retrofitted, and then it can be raised. The assembler takes no project-facts input, so a reinstall still requires the slots to be filled again by hand; the three rule-bearing regions are the part that must not drift, and they are now byte-comparable against the assembler output.
- **Rollback**: revert the commit, restore `pre_commit_gate.py` from `ng/tools/.backup/pre_commit_gate.py.bak-20260914210931`, and delete `.git/hooks/commit-msg`.
- **Related**: `CHANGELOG.md` entry for this date / `AGENTS.md` section 5 / `docs/rules/DEVELOP-FLOW.md` section 4.
## 20260914 · ai-generation-headers — Retrofit the provenance header onto the code files

- **Motivation**: the `provenance-marker` entry above made the `[AI-GENERATED]` header a rule but left the check at warning level, because 22 code files carried no header. The header cannot be written honestly without knowing the generating model and the date, so this entry supplies both values and takes the count to 0 of 22.
- **Scope**: 22 tracked code files. 17 live under `src/demo_gx/`, one under `scripts/`, four under `tests/`. One added comment line each. `docs/changes/engineering.md` records the change, and that edit is this entry itself.
- **Provenance values**: `model=deepseek-flash` and `reviewed_by=pending`. The date is the first appearance of the file in git, read with `git log --follow --format=%ad --date=short` and taking the oldest line. 15 files date to 2026-09-09 and 7 date to 2026-09-14.
- **Why two dates and not 2026-09-12**: the maintainer proposed 2026-09-12 as the approximate production date, and git disagrees with it. The ten pipeline modules arrived with the initial commit on 2026-09-09 under `pipeline/`. The six package `__init__.py` files and `validation/error_envelope.py` are new files of the 2026-09-14 source-layout refactor. A single date would therefore be wrong on every file in one direction or the other.
- **Behaviour and contract changes**: none. The header is a comment. `git diff --stat` reports 22 files changed, 22 insertions and 0 deletions.
- **Verification**: the header check reports 0 of 22 files missing. `.venv/bin/python -m compileall -q src scripts tests` exits 0 and `.venv/bin/python -m pytest -q` reports 27 passed. All 22 files keep LF with no carriage return. `cli.py` keeps its shebang on line 1 with the header on line 2. Every other file keeps its module docstring as the first statement, because a comment above a docstring does not displace it.
- **Known limits**: the date records the first appearance of a file in git, not every later AI edit to it. A file rewritten on a later day keeps its first date, and the change trail carries the later edits. `reviewed_by=pending` states that no human reviewer is on record for these files. The 40 percent review limit in section 5 of `AGENTS.md` is not meaningful on the seven one-line files, where the header is the whole of the added change.
- **Open item (no action taken)**: the header check is still a warning. Raising it to a blocker is a change to a shared tool and it would fire in every repository that installs the hook, so it waits for a decision.
- **Rollback**: revert the commit.
- **Related**: `CHANGELOG.md` entry for this date / `AGENTS.md` section 5 / the `provenance-marker` entry above.
## 20260914 · ruff-mypy-gate — Make the lint and type claims in CODING-STANDARD true

- **Motivation**: the rule set in `docs/rules/CODING-STANDARD.md` claimed two things the project did not do. Section 1 said every function carries type hints and that CI runs `mypy`. Section 1 also said `ruff` is the single lint and format tool, configured in `pyproject.toml`. Nothing was configured: `pyproject.toml` held no `[tool.ruff]` and no type checker, the dev extra held only `pytest`, `Makefile` had no lint target, `.gitlab-ci.yml` ran `pytest` only, and neither `ruff` nor `mypy` was installed. Read as a claim about this project, the rule file was false.
- **Scope**: `pyproject.toml` (dev extra, `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.mypy]` and one override), `Makefile` (a `lint` target), `.gitlab-ci.yml` (a `lint` job in the test stage), and 13 Python files.
- **Rule set**: `E`, `W`, `F`, `I`, `UP`, `B`, `SIM`, `C4`, `DTZ`, `G`, `BLE`, `RUF` at a line length of 120. The first run reported 52 findings. The formatter resolved all 21 line-length findings by itself. Nineteen findings were fixed automatically, covering import order, unused imports and redundant open modes.
- **Manual fixes**: nine logging calls in `cli.py` moved from f-strings to lazy `%` formatting, and the pipeline failure handler now uses `logger.exception`. Two handlers in `schema_validator.py` were narrowed from a blind `except Exception` and a bare `except` to `(TypeError, ValueError)`. The two sites are `json.loads` on a raw record and a `Series.astype(str)` conversion, which are the only exceptions those statements raise. The effect is that an unexpected exception now propagates instead of being swallowed, which is what section 4 of `AGENTS.md` asks for. Nine functions gained parameter or return annotations.
- **One documented exception**: the sample generator's base instant carries `# noqa: DTZ001`. The instant is deliberately offset-free, because the input contract carries the raw timestamp as a string and the Silver layer types it as UTC. Adding a `tzinfo` would change the generated payload.
- **Behaviour and contract changes**: none to the data contract. Log lines render the same text as before.
- **Verification**: `ruff check` exits 0, `ruff format --check` reports 22 files already formatted, `mypy` reports no issues in 18 source files, `pytest` reports 27 passed, and `make lint` runs all three. Behaviour equivalence was measured rather than assumed. The pipeline was run end to end from `HEAD` and from the working tree. Every Silver and Gold parquet file is identical in row count, column set and content hash. `metrics.json` is identical once the volatile run fields are dropped. The only differing artefact is the run-scoped error-envelope file name. A per-file AST comparison shows the structural diff is limited to annotations, logging calls, except clauses and imports, and that four files are AST-identical, meaning the formatter touched them and nothing else.
- **Known limits**: the formatter reformatted nine files, so the 40 percent review limit in section 5 of `AGENTS.md` is exceeded on the larger test files. The change is one mechanical formatter pass with no semantic content, which is the reason recorded here. `pandas` and `yaml` ship no type information and no stub package is installed, so the override ignores missing imports for those two modules and the frame boundary stays duck-typed.
- **Rollback**: revert the commit and reinstall the dev extra. The lint job fails on the reformatted files if they are reverted alone.
- **Related**: `CHANGELOG.md` entry for this date / `docs/rules/CODING-STANDARD.md` section 1 / `pyproject.toml`.
## 20260914 · shared-capability-path — Stop the rules material from naming a project layout

- **Motivation**: the shared rules material in the skill library named `utils/<module>.py` as the home of every shared capability. A concrete project path inside a template layer contradicts the assembler's own claim of assembling from three layers with zero project facts. It also made the rule unmeetable for any project that collects shared capabilities elsewhere. `demo-gx` is such a project: it uses `src/demo_gx/common/`, so 19 references in its assembled `docs/rules/` pointed at files that do not exist and never will.
- **Scope**: the upstream rules material in `project-doc-system/references/rules/`, both language layers. `python/PROJECT-STRUCTURE.md`, `python/CODING-STANDARD.md`, `_project-types/data-processing/PROJECT-STRUCTURE.md`, `_project-types/data-processing/CODING-STANDARD.md` and their four `en/` counterparts. Then the project's own `docs/rules/PROJECT-STRUCTURE.md` and `docs/rules/CODING-STANDARD.md`, re-assembled from the corrected material.
- **The change**: `utils/` becomes the `{shared}/` placeholder, which matches the placeholder style the rules already use for `{env}`, `{module}` and `{table}`. Each PROJECT-STRUCTURE document states the placeholder once: it names the cross-module capability directory, with `utils/` and `common/` as examples, and the module name follows the structure layer too.
- **Two rules sharpened while editing the same sentences**. The retry line is now conditional, so it reads "when a retry is needed it goes through the shared retry module" instead of implying that every project must own one. The note states that a capability the project genuinely does not need requires no module, which closes the trap of creating modules to satisfy a template.
- **Behaviour and contract changes**: none to code, config or artefacts. The project's `docs/rules/` four-piece set changes by 23 inserted and 19 deleted lines, and all of it is wording.
- **Verification**: the assembler's own check reports all four project rule files identical to the three-layer assembly, and its English pre-gate passes. Assembly was run in both language layers; the Chinese layer builds as well. A scan of the two affected layers reports 39 replaced references and the only remaining `utils/` occurrence is the example inside the new note.
- **Consequence for the open retry finding**: the earlier audit reported that `utils/retry.py` does not exist in this project. With the rule now path-free and conditional, that is no longer a conflict, so no deviation row is registered for it.
- **Known limits**: the skill library is not under version control, so this edit has no revert point of its own. The front-end layers still name `utils/request.ts` and `utils/formatXxx.ts`; they are the same class of problem and are left untouched because no project in that stack is under audit. The `java/` layer carries TypeScript content, which looks like a mislabelled directory.
- **Rollback**: revert the project commit and reassemble. The upstream edit has to be reverted by hand.
- **Related**: `CHANGELOG.md` entry for this date / `docs/rules/PROJECT-STRUCTURE.md` section 3 / skill `project-doc-system`.

## 20260914 · skeleton-conditional — Mark the skeleton conditional and re-cut the deviation table

- **Motivation**: the project's `docs/rules/PROJECT-STRUCTURE.md` described a project that does not exist. Its data-processing half was distilled from one Iceberg example project and kept that project's technology choices — Airflow, Iceberg, Spark SQL — as obligations of the project type, so `dags/`, `src/{pkg}/models/`, `src/{pkg}/catalog.py`, `sql/migrations/` and `sql/transforms/` read as mandatory. This project has none of them, and `AGENTS.md` section 1 states that there is no Iceberg and no catalog. A reader following the file looks for an entry point and for asset directories that are absent.
- **Scope**: the upstream rules material in `project-doc-system/references/rules/`, both language layers: `python/PROJECT-STRUCTURE.md` and `_project-types/data-processing/PROJECT-STRUCTURE.md`, plus their two `en/` counterparts. Then the project's `docs/rules/PROJECT-STRUCTURE.md` and `docs/business/KNOWN-ISSUE.md`.
- **Root cause**: the type layer generalized the package name to `{pkg}` and left the structure alone. What it carried over is a technology choice, not a property of the type: a data pipeline can be plain Pandas with no scheduler, no catalog and no SQL engine. The stack layer held a second contradiction of its own — section 1 put `config.py` at the package root while section 3 said the path follows the structure layer and placed its siblings under `{shared}/`.
- **The change**: every entry that depends on a technology choice now carries its condition on the same line. Examples are "only when a scheduler exists" and "only when there is a catalog". Both trees name the `AGENTS.md` project map as the authoritative source. `config.py` moves under `{shared}/` in both layers, which removes the section 1 versus section 3 contradiction and matches `src/demo_gx/common/config.py`. The layer names `ods`/`dwd`/`dws`/`ads` are marked as example naming while the dependency direction stays a red line. The two single-entry-point tables now carry per-row conditions, so a capability this project does not need no longer reads as a missing one.
- **Two further defects fixed in the same file**: the section 2 responsibilities table was split into two by a blockquote sitting inside it. Section 6 reached every assembly as an empty heading, because its whole body sat inside the `template-meta` block that assembly strips. The empty heading affected all four project types in both language layers.
- **Deviation table re-cut**: seven rows left `docs/business/KNOWN-ISSUE.md`, because upstream now marks those items conditional and states that not creating them is not a deviation. What remains are departures from an unconditional rule: the lock file, the coverage gate, the snapshot policy, amount precision, contract approval, the stage model and clock reads. The `#no-catalog` anchor stays, because it records a decision and an accepted impact, but it no longer claims an upstream mandate. The entry-point merge boundary moved out of the `No dags/` bullet into a bullet of its own.
- **Two machine checks added**: `verify_rules_layering.py` gained F1, which assembles every stack-and-type combination with the real assembler and fails on a heading with no body. It also gained F2, which walks the project's `docs/rules/` directory trees and requires every directory and file to exist or to carry its condition on the same line. F2 resolves placeholders as wildcards, and it supports a literal tree root such as `docs/changes/`.
- **Behaviour and contract changes**: none to code, configuration or artefacts. The project's `docs/rules/PROJECT-STRUCTURE.md` changes by 35 insertions and 23 deletions, and `KNOWN-ISSUE.md` by 10 insertions and 14 deletions. Upstream, the stack layer moves by 14 and 13 lines in each language layer and the type layer by 20 and 13, so the two language layers move by identical counts.
- **Verification**: the assembly check reports all four project rule files identical to the three-layer assembly. The layering self-check passes in both language layers and reports zero across all seven checks against this project. The English pre-gate passes, so the two language layers still agree on heading levels and fence counts. The new probe `test_rules_layer_gate.py` reports 15 of 15 and the existing assembly probe reports 10 of 10; the new probe covers both directions, including a ghost directory that must be reported and the same line with a condition that must pass. `pytest` reports 35 passed, `ruff check` exits 0, `ruff format --check` reports 24 files already formatted, `mypy` reports no issues in 19 source files, and the pre-commit gate exits 0.
- **Known limits**: the machine check covers directory trees only. Example names inside tables, such as `order_sync.py` in the naming conventions table, are type examples rather than claims about this project, and no mechanical rule separates the two, so that stays a human read. The skill library is not under version control, so the upstream edit has a tarball snapshot under `ng/backups/rules-material/` and no revert point of its own. The website, microservices and agent layers still state their trees without conditions, so adopting F2 in a project of those types would surface the same class of finding there.
- **Rollback**: revert the project commit and reassemble. The upstream edit has to be restored from the snapshot by hand.
- **Related**: `CHANGELOG.md` entry for this date / `KNOWN-ISSUE.md` "Skeleton Deviations" (deviation: environment setup) / `docs/rules/PROJECT-STRUCTURE.md` sections 1 and 3 / skill `project-doc-system`.

## 20260914 · system-scope-leak — Stop the project layers from mandating system-side files

- **Motivation**: three project-scope rule lines named files that belong to the Hermes system-script domain and stated them as obligations of a project. `path_anchor.py`, `path_resolve.py`, `load_shared_env()` and `path_governance_audit.py` all exist, but they live in the shared infrastructure directory: the anchor is the single truth source, the shim is the import entry and the audit is the guard for system scripts. This project has none of them, must not have them, and cannot satisfy either demand. The same table contradicted itself, because the row that mandates the anchor is followed by the row that says a project derives its anchor from `__file__` and does not depend on an external shared library.
- **Scope**: the upstream rules material, stack layer only, both languages: `python/PROJECT-STRUCTURE.md` for the single-entry-point table and the note beneath it, and `python/CODING-STANDARD.md` section 2. Then the project's `docs/rules/PROJECT-STRUCTURE.md` and `docs/rules/CODING-STANDARD.md`, re-assembled from the corrected material.
- **Root cause**: the previous pass replaced `utils/<module>.py` with the `{shared}/` placeholder and treated that as the whole class. It searched by path shape, and `path_anchor.py` carries no directory, so the pattern never matched. The deeper cause is scope rather than shape: the rules material serves two audiences, projects and system scripts, and the shared anchor is correct for the second one only. A line that mixes the two is right for nobody. An agent following it either creates a module the project does not need or routes project paths through a shim that is not on the project import path.
- **The change**: each line now states the project case first and keeps the system-script case as a qualified pointer. The path row reads "in a project: the project root anchor in the next row; system scripts: `path_anchor.py` / `path_resolve.py`". The red line drops the unconditional `load_shared_env()` requirement in favour of "a project derives its anchor from `__file__`". The note beneath the table scopes the guard: "changes to system scripts must pass `path_governance_audit.py` with zero violations". All three keep the pointer to skill `path-ssot-governance`, which is the correct home for the system side.
- **What is deliberately left alone**: the rows for `{shared}/retry.py`, `{shared}/db.py` and `{shared}/log.py`, and the absent condition markers on the table, are readability rather than correctness. The table preamble already states that the constraint is one implementation per capability rather than one file name, and that a capability the project does not need requires no module, so those rows claim no missing file. The name mismatch between `{shared}/log.py` and the project's `common/logger.py` is unchanged, and no deviation row is registered for it.
- **Behaviour and contract changes**: none to code, configuration or artefacts. Upstream, four files change by one or two lines each and every line count is unchanged, so the two language layers stay aligned. The project's `docs/rules/` changes by three lines across two files.
- **Verification**: the change surface was diffed against the pre-edit snapshot and is exactly the four upstream files, each with a line count identical to the snapshot. The assembler reports all four project rule files identical to the three-layer assembly and prints its no-project-facts line. The layering self-check reports zero across all seven checks. A scan of the project's `docs/` and `AGENTS.md` finds no unqualified occurrence of the system-side names, and the only surviving mentions carry the system-script qualifier plus the skill pointer. Both probes report 15 of 15 and 10 of 10, the pre-commit gate exits 0, and the project diff is three changed lines.
- **Known limits**: the `notebooks/` rows in both layer section 2 tables stay unconditional even though the project has no such directory, and section 11 of CODING-STANDARD handles the same subject correctly, so the two treatments disagree. The table rows still carry no condition markers, so a reader cannot tell an intentionally empty row from a forgotten one. The skill library is not under version control, so the upstream edit has a tarball snapshot under `ng/backups/rules-material/` and no revert point of its own.
- **Rollback**: revert the project commit and reassemble. The upstream edit has to be restored from the snapshot by hand.
- **Related**: `CHANGELOG.md` entry for this date / `docs/rules/PROJECT-STRUCTURE.md` section 3 / `docs/rules/CODING-STANDARD.md` section 2 / skill `project-doc-system` / skill `path-ssot-governance`.

## 20260914 · logger-module-name — Name the shared logging entry point `logger.py`

- **Motivation**: the rules layer asserted the shared logging entry point as `{shared}/log.py`. The project's module is `common/logger.py`. Four mentions existed in the assembled set and one of them was a red line, "uniformly use `{shared}/log.get_logger`". For this project that resolves to `demo_gx.common.log`, a module that does not exist, so an agent following the red line writes an import that raises ImportError. The name was also the odd one out everywhere else: `MODULE-DESIGN.md`, `PROJECT.md`, `KNOWN-ISSUE.md`, `README.md`, `docs/archive/module-design.md` and `src/demo_gx/cli.py` all say `logger.py`. The count was one against ten, and the one side was the layer that is forbidden to carry project facts.
- **Scope**: the upstream rules material, four files, both languages: `python/PROJECT-STRUCTURE.md` for the tree comment and the table row, `python/CODING-STANDARD.md` section 1 for the red line, and `_project-types/data-processing/PROJECT-STRUCTURE.md` for the table row. Then the project's `docs/rules/PROJECT-STRUCTURE.md` and `docs/rules/CODING-STANDARD.md`, re-assembled from the corrected material. No code changed.
- **Why this is not a project fact written into a template**: the rule against project facts guards tokens that pin a template to one project, such as the package name, an environment path or a build command. `logger.py` is a generic name that any project can hold. It is also the better template choice on naming merits alone. Every other row names the capability as a bare noun, such as `config`, `mask`, `retry` or `db`. `log` instead names the output artefact, not the module that provides `get_logger`. The test applied was whether the edit stands up with this project deleted, and it does.
- **The boundary this change does not cross**: the template is the record. If a later project names its logging module something else, the correct handling is a deviation row on the project side, not another edit here. Editing the template to follow each project would turn a shared layer into a summary of whoever complained last.
- **Verification**: the replacement was assertion-guarded, with each old string required to occur exactly once before anything was written. All four files kept their line counts. The old names now count zero and the new name counts where expected. The assembler reports the four project rule files identical to the three-layer assembly. The layering self-check reports zero across all seven checks and the English pre-gate exits 0. Both probes report 15 of 15 and 10 of 10. The project diff is four lines across two files, `make lint` passes, `pytest` reports 35 passed and the pre-commit gate exits 0.
- **An inventory of what the rules layer now names**: the shared entry points that exist are `config.py`, `logger.py` and `mask.py`, each resolving to a real file under `src/demo_gx/common/`. The ones that do not exist are `db.py`, `money.py`, `quality.py` and `retry.py`, and the table preamble already covers them, because a capability the project does not need requires no module. Whether those rows need condition markers stays open.
- **Known limits**: this is the third round in which the first inventory of a file-name class was incomplete. The earlier pass over the same month listed two absences and missed `money.py` and `quality.py`, because it searched by pattern rather than by name list. A scanner that resolves every shared entry-point mention against the project, and flags names that sit one edit away from a real file, would have caught `log.py` against `logger.py` without a human read. It is proposed and not built.
- **Rollback**: revert the project commit and reassemble. The upstream edit has to be restored from the snapshot by hand.
- **Related**: `CHANGELOG.md` entry for this date / `docs/rules/CODING-STANDARD.md` section 1 / `docs/rules/PROJECT-STRUCTURE.md` sections 1 and 3 / skill `project-doc-system`.
## 20260914 · doc-repo-alignment — Align the project documents with the repository

- **Motivation**: three places where the documents named something the repository does not match. The test count was stale in four places: they said 27 tests while `pytest --collect-only` reports 35. This round's own PII work added those eight tests, so the drift was fresh rather than old. The two project trees each missed entries, and the storage tree used a placeholder in place of a path.
- **Scope**: `README.md` plus four documents under `docs/business/`: `PROJECT.md`, `KNOWN-ISSUE.md`, `DATA-DESIGN.md` and `INTERFACE-DESIGN.md`. No code, no configuration and no artefact changed.
- **The change, in three parts**: (1) the test count became 35 in all four places. (2) Both trees were refreshed and both were kept, which is the user's decision. `mask.py`, `.gitignore` and `AGENTS.md` were missing from both. The two hook files were missing from `PROJECT.md` alone. The `Makefile` comment gained the `lint` target and the CI comment gained the `lint` job. (3) The storage tree now starts at `data/dev/output/` instead of `{storage.base_path}/output/`, and it names the values the sample data uses for `source_system`. One sentence beneath it lists three real paths a dev run writes.
- **Why the abstract form was a defect**: `{storage.base_path}` resolves only by opening a second document. DATA-DESIGN.md section 2.1 pointed at INTERFACE-DESIGN.md section 3.1 for the mapping. Those two documents are a contract pair, so a reader of either one should be able to name a path.
- **A separate defect found on the way**: three markdown files were LF in the working tree while `.gitattributes` declares `*.md eol=crlf`. A text-mode read and write in the editing path converts CRLF to LF on every save, which this round reproduced on DATA-DESIGN.md. The gate's line-ending check reads bytes across every tracked file, so the class is machine-visible. It now reports zero blocking findings and zero warnings.
- **Behaviour and contract changes**: none. No path, name or value changed meaning. Five files changed by 26 insertions and 18 deletions.
- **Verification**: a validator read both trees, resolved every entry against the working tree and checked both directions. It reports 28 entries per tree, zero entries missing on disk and zero tracked files left uncovered. `pytest` reports 35 passed, `ruff check` exits 0, `ruff format --check` reports 24 files already formatted, `mypy` reports no issues in 19 source files and the pre-commit gate exits 0. The line-ending check reports zero findings.
- **Follow-up in the same round**: `make lint` is now a row in the README quick start and a command under `PROJECT.md` verification commands. The manual equivalents are listed beside it. This closes one of the two findings that sat outside the first commit.
- **Known limits**: the tree validator is an ad-hoc script and is not part of the gate. Two trees now describe one repository, so a new file needs an edit in both places and nothing enforces that. `.gitlab-ci.yml` line 2 points at `PROJECT.md` section 9, which does not exist, so that cross-reference stays open.
- **Rollback**: revert the commit.
- **Related**: `CHANGELOG.md` entry for this date / `docs/business/PROJECT.md` directory layout / `docs/business/DATA-DESIGN.md` section 2.1 / `docs/business/INTERFACE-DESIGN.md` section 3.1.
## 20260914 · check-data-verifier — Add a read-only verifier for the run output

- **Motivation**: nothing compared an artefact with the contract that describes it, and the most natural way to read a Gold partition fails. `pd.read_parquet("gold/fact_daily_events")` raises `ArrowTypeError: Unable to merge: Field event_date has incompatible types: date32[day] vs dictionary<string>`. A partition directory name is Hive style, so pyarrow infers a string column named `event_date`, while the file already carries that column as `date32`. The same cause breaks `pq.read_table(directory)` and a glob over every partition file. Only one partition at a time, or a dataset built without partition inference, reads cleanly.
- **Scope**: a new `scripts/check_data.py`; a `check-data` target in `Makefile`; `scripts` added to the pytest import path in `pyproject.toml`; a `pyarrow` entry in the mypy override; and a new `tests/test_check_data.py`. No pipeline module changed.
- **How a layer and a table are selected**: the layer is a parameter, because the layers are structural. The table is discovered from disk. Under Gold every sub-directory is one table, so a table added to the pipeline is checked without editing the tool. Silver holds one dataset and Bronze one raw archive, so each reports a single block. Every discovered table is paired with `docs/tables/{table}.md`, and a contract with no artefact is reported as well.
- **Checks**: file and partition counts, row counts, column and type parity, the declared partition key, business primary key uniqueness, and a mask check for every field in `pii.masked_fields`. Bronze adds a JSON-per-line parse. The quarantine reports the record count and the `error_type` split.
- **Path handling**: layer roots come from the environment config, never from a literal, and the tool resolves relative config paths against the project root. Both follow red lines 5 and 6.
- **Two contract numbers corrected by hand**: the tool prints measured values and never parses prose, so the same defect class had to be read. `silver_events.md` claimed 102 rows and about 16 KB per partition. Measured: 101 rows and about 11 KB. Both were corrected, and the other four contracts were checked against their artefacts.
- **Negative verification**: a verifier that only ever sees compliant input proves nothing. Ten probes in `tests/test_check_data.py` point the tool at a throw-away project tree and pin both directions. The compliant case must pass, and each injected defect must change the exit code and name its check. The injected defects are an undeclared column, a declared column that is absent, a type drift and a duplicate primary key. They also include a plaintext PII value, a snapshot contract meeting partition directories, a missing contract, an unknown table name and an empty scope.
- **Zero scope is not a pass**: an empty selection exits 3 and prints `NOT SCANNED`, so a run that inspected nothing cannot be mistaken for a run that inspected everything.
- **An unrun environment is a skip, not a failure**: `metrics.json` is the marker a completed run leaves behind. Without it the tool exits 3 and says the environment has never run. The first draft reported Bronze and Silver as failed for `env=prod`, which is a false red: nothing is broken, the pipeline simply has not run there.
- **A contract with no table fails once a run happened**: the same run marker decides this. Every table a contract describes should be on disk after a run, so an absent one is a failure rather than a footnote.
- **Levels**: a contract mismatch, an unreadable artefact, a broken key or a leaked PII value fails and sets exit 1. A missing contract, an unreadable partition section and a missing primary key declaration warn. Bronze reports no contract as normal rather than as a warning, because that is the designed state.
- **Evidence**: 45 tests pass. `ruff check` exits 0, `ruff format --check` reports 26 files formatted, `mypy` reports no issues in 20 source files and the pre-commit gate exits 0.
- **Known limits**: the primary key check holds key values in memory, so it is a demo-scale check; `--event-date` narrows it to one partition. The tool reads only the columns it needs, one file at a time. Contract prose stays human-read.
- **Rollback**: revert the commit. The tool writes nothing, so no artefact needs rebuilding.
- **Related**: `docs/tables/silver_events.md` / `docs/business/PROJECT.md` verification commands / `README.md` section "Inspecting the Output" / `AGENTS.md` section 9.1 project map.
## 20260914 · show-data-viewer — Add a read-only viewer for the run output

- **Motivation**: `make check-data` reports statistics only. It answers "is this run compliant" and never shows a row. A reader asking "what does the data look like" had no command, and the manual path is awkward. `pd.read_parquet("gold/fact_daily_events")` raises `ArrowTypeError`, and the pyarrow form that works needs an explicit `date32` partition type.
- **Scope**: a new `scripts/show_data.py`; a `show-data` target in `Makefile`; a new `tests/test_show_data.py`; two helpers in `scripts/check_data.py` renamed to public names and one extracted. No pipeline module changed and no artefact changed.
- **Why a second tool instead of a flag**: showing and judging are two jobs with two contracts. The verifier must exit non-zero on a defect, because CI reads its status. A viewer must never do that, because a table that breaks its contract is exactly the table someone wants to look at. One file would carry two output shapes and two exit conventions.
- **Sharing**: `check_data.py` owns where the data lives. `parquet_files` resolves the partitioned and the snapshot layout, and `show_data.py` calls it. The renames of `_resolve` and `_partition_dirs` are what let the viewer import them without reaching into private names. All twelve existing probes still pass, so the extraction changed no behaviour.
- **Selection**: `--layer`, `--table` and `--event-date` mean the same as in the verifier. `--limit` caps the rows, `--columns` projects, `--schema` prints names and types, and `--format` chooses table, json or csv.
- **Streams**: with json or csv the rows go to stdout and the report moves to stderr, so a pipe carries data only. Usage errors stay on stdout, so they survive a redirected stderr.
- **Exit status**: 0 something was shown, 2 the request does not match the data, 3 nothing to look at. There is no failure status.
- **Reading**: partitions are chosen before any file is opened, and rows are read batch by batch. A small limit stops after the batches it needs, so a peek never scans the table. No clock is read, so the same artefacts print the same rows.
- **Two dishonest report lines removed during the round**: the first draft printed `showing event_date=X` while it could read several partitions. It also counted snapshot files as "partition files". The line now names every partition the shown rows came from.
- **Negative verification**: seventeen probes point the tool at a throw-away project tree. They pin the stored values reaching the screen, the limit, the projection, the date selection, an unknown column, an unknown table, the schema mode reading no rows, both machine formats, an empty partition, an unrun environment and the early stop. Two of them pin the separation from the verifier: a plaintext PII value and a missing contract must not fail the viewer.
- **Evidence**: 64 tests pass. `ruff check` exits 0, `ruff format --check` reports 28 files formatted, `mypy` reports no issues in 21 source files and the pre-commit gate exits 0.
- **Known limits**: a wide table prints rows a thousand characters long, so `silver_events` is hard to read without `--columns`. Cell truncation is a candidate for a later change. The viewer reads a contract only to print its path.
- **Rollback**: revert the commit. The tool writes nothing, so no artefact needs rebuilding.
- **Related**: `README.md` section "Inspecting the Output" / `docs/business/PROJECT.md` verification commands / `AGENTS.md` section 9.1 project map.
## 20260914 · case-insensitive-selectors — Read the make selectors in either case

- **Motivation**: a selector typed in lowercase was silently dropped. `make show-data DATE=2026-09-01` read one partition; `make show-data date=2026-09-01` read the whole table and still exited 0. The interface gave no hint, because make leaves an unassigned variable alone and a name the makefile never reads looks like a name that was honoured.
- **Scope**: the selector block in `Makefile`; a new `tests/test_makefile_selectors.py`; a note and two examples in `README.md`. No pipeline module and no artefact changed.
- **Mechanism**: every selector is now assigned from its lowercase twin with `?= $(or $(lower),default)`. `LAYER=` and `layer=` resolve to the same value, and the lowercase spelling can carry the documented default. When both spellings are given the uppercase one wins, because `?=` is what refuses to overwrite a command-line assignment. `check-data` and `show-data` share the block, so both commands gained it in one change.
- **Why not full case folding**: make compares variable names exactly and has no case-folding function. Folding would cost a shell call per selector while the makefile is parsed, to accept a spelling nobody typed (`Layer=`). Mixed case therefore stays a typo, and the selector block says so in place.
- **Negative verification**: thirteen probes in `tests/test_makefile_selectors.py`. A dry run prints the recipe without running it, including the `@`-silenced `show-data` recipe, so each probe reads the argv make would build while `data/` stays untouched. They pin six behaviours. One selector spelled either way builds the same argv, a lowercase selector carries its value, and an empty selector drops its argument. The documented defaults survive when nothing is given, the uppercase spelling wins when both are given, and `check-data` reads the same block.
- **Evidence**: 77 tests pass. `ruff check` exits 0, `ruff format --check` reports 29 files formatted, `mypy` reports no issues in 21 source files and the pre-commit gate exits 0.
- **Known limits**: make imports the whole environment into its variable table, so an *exported* variable that shares a selector's name still reaches that selector. `COLUMNS` is the realistic one, since a shell sets it for terminal width. Closing that is a separate change: read only command-line assignments.
- **Rollback**: revert the commit. No target recipe and no tool argument changed, so no artefact needs rebuilding.
- **Related**: `README.md` section "Inspecting the Output" / `Makefile` selector block / `tests/test_makefile_selectors.py`.
## 20260915 · command-line-only-selectors — Only a typed value becomes a selector

- **Motivation**: two ways for a selector to be read by accident. make imports the whole environment into its variable table, so an exported `COLUMNS` — which shells set for terminal width — reached `show-data` as `--columns 80`, and the viewer answered `unknown column 80`. Separately, `SCHEMA` was tested for emptiness, so `SCHEMA=0` turned the flag on.
- **Scope**: the selector block in `Makefile` and the `show-data` recipe; six new probes in `tests/test_makefile_selectors.py`; a note in `README.md`. No pipeline module and no artefact changed.
- **Mechanism**: `$(call cmdline,NAME,default)` returns `NAME` only when `$(origin NAME)` is `command line`. Each selector then resolves as uppercase, then lowercase, then default: `ENV := $(call cmdline,ENV,$(call cmdline,env,dev))`. The test reads `$(origin)` rather than the value, so a typed `TABLE=` stays empty instead of falling through to a default.
- **Behaviour change**: exporting a variable no longer sets a selector. `ENV=test make run` still works, because it is a command-line assignment. `export ENV=test` before `make run` no longer does. The README keeps the command-line form, and the CI pipeline calls the tools directly rather than through make, so nothing depended on the exported form.
- **SCHEMA became a switch**: `1 yes true on` turn it on, `0 no false off` turn it off, and any other value stops the build with a message naming the accepted words. The flag is built once into `SCHEMA_FLAG`, so the recipe no longer decides what counts as true.
- **Negative verification**: six probes. An exported `COLUMNS`, `ENV`, `TABLE`, `FORMAT` or `LIMIT` must leave the command at its defaults. An exported lowercase twin must be ignored too. Each on-word must add `--schema` and each off-word must not. A value that is neither off nor on must fail the build, asserted on the exit code and on the message.
- **A first draft that had to go**: the helper was written across two lines with a backslash continuation. make collapses the backslash and the line break into a space inside a definition, so every unset selector resolved to a single space and the new `SCHEMA` message stopped every build. The helper now fits on one line, and the probe that catches it is the one asserting the defaults survive.
- **Evidence**: 88 tests pass. `ruff check` exits 0, `ruff format --check` reports 29 files formatted, `mypy` reports no issues in 21 source files and the pre-commit gate exits 0.
- **Rollback**: revert the commit. No artefact needs rebuilding, and an exported variable simply stops selecting again.
- **Related**: `README.md` section "Inspecting the Output" / `Makefile` selector block / `tests/test_makefile_selectors.py`.
## 20260915 · show-naming-completion — Finish the rename inside the viewer

- **Motivation**: the tool was renamed to `show` two changes earlier, in `show_data.py` and `make show-data`, but two functions inside the file still carried the old `peek` prefix. Nothing failed, and a search for the new name cannot find an old name: the tests call `main()`, so no test ever read either identifier.
- **Scope**: `scripts/show_data.py` only. `peek_parquet` and `peek_json` become `show_parquet` and `show_json`, at both definitions and both call sites. No behaviour changes.
- **Verification**: a repository-wide search for `peek` finds nothing left in `scripts/`, `src/` or `tests/`. Both new names import and print from a fresh interpreter. Every output path was then run against the real artefacts: the Parquet table path, the JSON Lines paths for Bronze and for the error quarantine, `FORMAT=json`, `FORMAT=csv` and `SCHEMA=yes`.
- **Evidence**: 88 tests pass. `ruff check` exits 0, `ruff format --check` reports 29 files formatted, `mypy` reports no issues in 21 source files and the pre-commit gate exits 0.
- **Rollback**: revert the commit or rename back. Nothing outside the file refers to either name.
- **Related**: `scripts/show_data.py` / the `show-data-viewer` entry in this file.
## 20260915 · config-layout-propagation — Carry the config layout through the code, the tests and the documents

- **Motivation**: the configuration files left the flat layout — environment files to `config/env/{env}.yaml`, the data contract to `config/data/schema.yaml` — and nothing that reads them moved with them. `python -m demo_gx.cli` stopped at `FileNotFoundError: config/dev.yaml not found`; sixteen validation tests errored on the same missing contract; ruff reported one whitespace error and four unformatted files. Sixteen document references still named the old paths, and `AGENTS.md` still called its two maps `§9.1` and `§9.2` while carrying a rule-provenance section the project had dropped.
- **Scope**: `src/demo_gx/common/config.py` (both read paths and their docstrings), `tests/test_validation.py` (the schema fixture), `src/demo_gx/validation/schema_validator.py` (module docstring); whitespace left inside `src/demo_gx/cli.py`, `src/demo_gx/common/metrics.py`, `src/demo_gx/common/time_utils.py` and `src/demo_gx/validation/schema_validator.py`; `AGENTS.md`, `README.md` and five business documents; `docs/rules/{PROJECT-STRUCTURE,DEVELOP-FLOW}.md`, re-assembled from the upstream three layers.
- **Behaviour and contract changes**: none to the pipeline. The layout itself is a published contract — `INTERFACE-DESIGN.md` section 3.1 names the configuration file per environment and `MODULE-DESIGN.md` section 3.1 lists the configuration items — so both were updated and now agree with the code. `AGENTS.md` numbers its maps `§9` and `§10` in the same heading form as the sections above them. The rule-provenance section is gone: that text described the assembly tool, which is maintainer knowledge. The deviation register is now named in the `§9` project map instead. `KNOWN-ISSUE.md` no longer calls `AGENTS.md` section 10 the pointer; the bridge sentence that maps historical change entries citing that section onto this file is kept and marked as historical.
- **Verification**: `.venv/bin/python -m pytest tests/ -q` reports 88 passed, against 72 passed and 16 errors before. `.venv/bin/python -m demo_gx.cli --env dev` exits 0 with `Pipeline completed successfully.`. `ruff check` exits 0, `ruff format --check` reports 29 files formatted, and `mypy` reports no issues in 21 source files. `python3 ng/tools/pre_commit_gate.py --repo . --agents-strict` exits 0 with no warnings. `python3 ng/tools/rules_assembly.py --stack python --type data-processing --lang en --check .` reports all four rule files identical to the upstream assembly. `python3 ng/tools/verify_rules_layering.py --project . --stack python --type data-processing --lang en` reports 0 findings on every row, and `agents_assembly.py --verify` passes. All nine changed Markdown files hold CRLF with zero bare LF and zero double CR. A repository-wide search for the five old path spellings finds nothing outside `docs/archive/` and `docs/changes/`.
- **Rollback**: reverting this commit alone returns the code to the old paths while the files stay in the new layout, which is the broken state this entry closes. A real rollback also moves `config/env/` and `config/data/` back to the flat layout, re-runs `make check-data` and the four rule checks, and re-assembles `docs/rules/`. No artefact needs rebuilding.
- **Related**: `docs/business/CHANGELOG.md` (its one-line entry is written by the next changelog commit: the file lists commit hashes, and a commit cannot list its own) / branch `fix/config-layout-propagation`.
## 20260915 · inspection-cli-contract — Publish the inspection commands and close two rule drifts

- **Motivation**: three gaps left by the previous change. The two read-only inspection commands lost their only written home when the README was trimmed, and their exit codes, selector rules and stream policy are part of the interface. `KNOWN-ISSUE.md` cited a merge boundary that `docs/rules/` no longer states. `docs/rules/CODING-STANDARD.md` still sent system scripts to `path_anchor.py`, which `PROJECT-STRUCTURE.md` had stopped doing.
- **Scope**: `docs/business/INTERFACE-DESIGN.md` (new section 1.1); `docs/business/KNOWN-ISSUE.md` (the entry-point applicability boundary); `docs/rules/CODING-STANDARD.md`, re-assembled from the upstream three layers.
- **Behaviour and contract changes**: no code, no configuration and no artefact changed. The interface contract gains the inspection commands. It records the four shared selectors, the case and origin rules, the four `show-data` selectors, and the `SCHEMA` switch words. It also records both exit-code tables, the stream split for machine formats, what `check-data` checks, and what `show-data` refuses to do. It notes that make reports its own `2` for any non-zero tool exit. `KNOWN-ISSUE.md` now explains why the entry point sits inside the package without leaning on a withdrawn rule. `CODING-STANDARD.md` keeps the project-side path rule and drops the system-script clause, so the two rule files state the same thing again.
- **Verification**: every documented behaviour was probed. `.venv/bin/python -m pytest tests/ -q` reports 88 passed. `make check-data` reports 7 tables, 0 FAIL, 0 WARN. Called directly, `scripts/check_data.py --table nope` returns 2, `--event-date 1999-01-01` returns 1, and `--env prod` returns 3. `scripts/show_data.py --format json` over one table returns 0, `--event-date 1999-01-01` reports the missing partition and returns 0, and `--env prod` returns 3. `make show-data SCHEMA=1` prints names and types without reading rows, `SCHEMA=bogus` stops the build, and a lowercase `date=` selects the same partition as `DATE=`. `python3 ng/tools/rules_assembly.py --stack python --type data-processing --lang en --check .` reports all four rule files identical to the upstream assembly. `python3 ng/tools/verify_rules_layering.py --project . --stack python --type data-processing --lang en` reports 0 findings on every row, and the pre-commit gate exits 0 with no warnings. The three changed Markdown files hold CRLF with zero bare LF and zero double CR.
- **Rollback**: revert the commit. No code path and no artefact reads these three documents.
- **Related**: `docs/business/CHANGELOG.md` / branch `fix/config-layout-propagation`.
## 20260915 · config-layout-in-the-trees — Show the configuration split instead of squashing it

- **Motivation**: the config move left both layout trees describing a two-level directory in one comment line. A reader of `README.md` or `PROJECT.md` could not see the shape of `config/` from the tree, and the business-document templates still advertised `data/ cfg/ logs/ ...`.
- **Scope**: `README.md` and `docs/business/PROJECT.md` (the layout trees); `docs/rules/PROJECT-STRUCTURE.md`, re-assembled from the upstream three layers, whose data-processing skeleton now shows the same subtree.
- **Behaviour and contract changes**: none to code, configuration or artefacts. `config/` is now a subtree in all three trees: the directory line states the split, and one child line each for `env/` and `data/`. The two business-document templates carry the same shape, so a new project starts from it rather than inventing one. The README template keeps a one-level view and states that expanding it into a full tree is forbidden, because the full tree lives in `PROJECT.md` and a copy drifts.
- **Verification**: every tree comment starts at the same column, asserted in the edit script for the two project trees. `python3 ng/tools/rules_assembly.py --stack python --type data-processing --lang en --check .` reports all four rule files identical to the upstream assembly, and `verify_rules_layering.py --project . --stack python --type data-processing --lang en` reports 0 findings on every row. A negative probe on a copy of the repository rewrites the nested line to `data/nope.yaml`; `verify_rules_layering.py` then exits 1 and names that path, so the subtree line is really checked and not merely skipped. `.venv/bin/python -m pytest tests/ -q` reports 88 passed, ruff and mypy are clean, and the pre-commit gate exits 0 with no warnings. A byte scan of every tracked markdown and code file finds no line-ending violation.
- **Rollback**: revert the commit. These three files carry no executable behaviour.
- **Related**: `docs/business/CHANGELOG.md` / branch `fix/config-layout-propagation`.
